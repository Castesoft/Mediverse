export class AuthFormStep {
  number: number;
  title: string;
  subtitle: string;

  constructor(number: number, title: string, subtitle: string) {
    this.number = number;
    this.title = title;
    this.subtitle = subtitle;
  }
}
